package app.concurrency;

import app.runner.Runner;

public class CompletableFutureTest {
    public static void main(String[] args) {
        Runner.runAnnotatedMethods(CompletableFutureTest.class);
    }
}
